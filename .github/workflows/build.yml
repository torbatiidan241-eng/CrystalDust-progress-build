name: Build CrystalDust
on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Configure Git
        run: |
          git config --global core.symlinks false
          git config --global core.longpaths true

      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential libpng-dev gcc-arm-none-eabi make

      - name: Build Project Tools
        run: |
          # 1. Build the special agbcc compiler
          cd tools/agbcc
          ./build.sh
          ./install.sh ../
          cd ../..
          
          # 2. Build the remaining tools
          make tools
          
          # 3. Now that they are built, set permissions
          chmod +x tools/agbcc/bin/agbcc
          chmod +x tools/gbagfx/gbagfx
          chmod +x tools/scaninc/scaninc
          chmod +x tools/ramscrgen/ramscrgen
      - name: Build ROM
        run: |
          # Manually create the build directory to avoid 'mkdir' errors
          mkdir -p build/emerald
          # Execute make with explicit tool paths
          make
        env:
          # Keep system paths first, then append tool paths
          PATH: /usr/bin:/bin:/usr/local/bin:${{ github.workspace }}/tools/agbcc/bin:${{ github.workspace }}/tools/gbagfx:${{ github.workspace }}/tools/scaninc:${{ github.workspace }}/tools/ramscrgen:${{ github.workspace }}/tools/bin2c:${{ github.workspace }}/tools/gbafix:$PATH

      - name: Upload ROM Artifact
        uses: actions/upload-artifact@v4
        with:
          name: crystal-dust-rom
          path: '*.gba'
