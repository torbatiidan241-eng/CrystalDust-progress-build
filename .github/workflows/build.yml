name: CrystalDust Build

on:
  push:
    branches:
      - progress

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y binutils-arm-none-eabi gcc-arm-none-eabi libpng-dev make git

      - name: Build agbcc
        run: |
          cd tools/agbcc
          chmod -R +x .
          # Force the build and explicitly install the binaries
          ./build.sh
          # Some versions of agbcc need an install step, or manual move
          mkdir -p bin
          cp gcc/agbcc bin/agbcc
          cp gcc/xgcc bin/xgcc
          cd ../..
      - name: Build ROM
        run: |
          # Explicitly set the compilers to the arm-none-eabi versions
          make CC=arm-none-eabi-gcc AR=arm-none-eabi-ar LD=arm-none-eabi-ld OBJCOPY=arm-none-eabi-objcopy -j$(nproc)

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: CrystalDust-ROM
          path: '*.gba'
