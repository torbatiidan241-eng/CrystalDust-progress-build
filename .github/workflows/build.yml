name: CrystalDust Build

on:
  push:
    branches: [ progress ]
  pull_request:
    branches: [ progress ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-9 g++-9 gcc-arm-none-eabi binutils-arm-none-eabi libpng-dev make git

      - name: Build Toolchain
        run: |
          cd tools/agbcc
          chmod -R +x .
          
          # 1. Setup Environment
          export CC=/usr/bin/gcc-9
          export CXX=/usr/bin/g++-9
          # Inject -Wa,-mcpu=arm7tdmi to force assembler compatibility
          export CFLAGS="-std=gnu89 -w -fno-strict-aliasing -fno-builtin -Wno-format-security -fcommon -Wa,-mcpu=arm7tdmi"
          
          # 2. Patch Makefile dynamically to ensure correct assembler usage
          # This finds the specific Makefile that builds libgcc and forces the assembler
          find . -name "Makefile" -exec grep -l "libgcc1" {} + | xargs -r sed -i 's|$(AS)|arm-none-eabi-as -mcpu=arm7tdmi|g'
          
          # 3. Build agbcc
          ./build.sh
          
          # 4. Build libraries
          # We use a shim to ensure the build process picks up our newly built compiler
          mkdir -p ../shim
          echo '#!/bin/sh' > ../shim/agbcc
          echo 'exec '"$(pwd)"'/bin/agbcc "$@"' >> ../shim/agbcc
          chmod +x ../shim/agbcc
          export PATH=$(pwd)/../shim:$PATH
          
          [ -d "libc" ] && cd libc && make -j$(nproc) CFLAGS="$CFLAGS" && cd ..
          [ -d "libgcc" ] && cd libgcc && make -j$(nproc) CFLAGS="$CFLAGS" && cd ..
          cd ../..
          
          # 5. Build Host Utilities (preproc, etc.)
          # UNSET environment variables to prevent host tools from inheriting ARM flags
          unset AS CC CXX AR CFLAGS
          
          find tools -mindepth 2 -name "Makefile" -exec dirname {} \; | while read dir; do
            if [ "$dir" != "tools/agbcc" ]; then
              make -C "$dir" CC=/usr/bin/gcc CXX=/usr/bin/g++ AS=/usr/bin/as AR=/usr/bin/ar CFLAGS="-std=gnu11 -O2 -Wall"
            fi
          done

      - name: Build ROM
        run: |
          export PATH=$(pwd)/tools/agbcc/bin:$PATH
          export AGBCC=$(pwd)/tools/agbcc/bin/agbcc
          make -j$(nproc)

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: CrystalDust-ROM
          path: '*.gba'
